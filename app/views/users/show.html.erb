<% if user_signed_in? %>
<h1>Welcome <%if current_user.name != nil %><%=current_user.name%> <%else%><%=current_user.email%><%end%>!</h1>   <h3>Here are your Blog Posts:</h3><% current_user.posts.each do |post| %>
  <ul>
    <li><%= link_to post.title, post_path(post) %></li>
  </ul>
  <% end %><br>
  <h4><%= link_to "New Post", new_post_path(@path) %></h4>
  <h4><%= link_to "View All Public Posts", posts_path %></h4>
  <h4><%= link_to "View Categories", categories_path %>  </h4>
  <% end %>

 
    <% if current_user.admin? %>
      <h4><%= link_to "Create Category", new_category_path %><br>
    <% end %>
